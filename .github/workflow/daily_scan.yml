name: Daily Market Scanner

on:
  schedule:
    # Runs at 3:30 UTC (9:00 AM IST) -> Morning News
    - cron: '30 3 * * *'
    # Runs at 7:30 UTC (1:00 PM IST) -> Mid-Day Update
    - cron: '30 7 * * *'
    # Runs at 10:30 UTC (4:00 PM IST) -> Market Report
    - cron: '30 10 * * *'
  workflow_dispatch:

jobs:
  run-scanner:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      # FORCE PYTHON 3.12 (Hardcoded)
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12' 

      # HARDCODED INSTALLS (Ignoring requirements.txt)
      # We are installing 'pandas-ta-classic' which works everywhere
      - name: Install libraries
        run: |
          python -m pip install --upgrade pip
          pip install yfinance pandas supabase feedparser requests beautifulsoup4
          pip install pandas-ta-classic

      - name: Run Market Report
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          python market_report.py
          python fetch_rss_news.py

